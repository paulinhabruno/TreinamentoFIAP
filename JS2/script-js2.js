const form = document.querySelector("#form-notes");

const handleSubimit = (event) => {
    event.addEventListener();

    //recuperar a nota digitada pelo usuário

    //criar um article com esta nota

    //adicionar o artigo na section

    //atualizar o local storage
}

form.addEventListener("submit", handleSubimit);